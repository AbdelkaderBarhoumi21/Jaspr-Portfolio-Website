// TODO: Export `const String animationScripts = r'''...''';` containing:
//   - IntersectionObserver that toggles .is-visible on .reveal-* elements
//   - Typewriter cycle for .typewriter elements (reads data-words)
//   - Navbar scroll behavior (toggles .is-scrolled past 40px)
//   - ScrollSpy (highlights active nav link based on visible section)
//   - 3D tilt on .tilt elements
//   - Custom cursor (dot + lagging ring), hidden on touch devices
// Injected once via <script> tag in main.server.dart Document(head: [...]).
